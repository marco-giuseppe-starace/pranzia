# URL base delle API del backend Laravel. Relativo di proposito: cosi'
# la richiesta parte sempre dalla stessa origin con cui e' stata caricata
# la pagina (localhost, IP LAN, tunnel), e passa dal proxy /api di Vite
# verso il backend invece di dover risolvere un dominio fisso.
VITE_API_URL=/api
